# 🏴‍☠️ Environment Variables for Communication Analyzer
# Copy this file to .env and modify as needed

# Docker mode (set to true when running in Docker)
DOCKER_MODE=true

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Google Sheets settings
DEFAULT_SHEET_TITLE=WhatsApp Chat Analysis
DEFAULT_SHARED_EMAIL=your-email@example.com

# File paths (use ./credentials/ or your paths)
CLIENT_SECRETS_PATH=./credentials/service-account.json
WHATSAPP_FILE_PATH=./path/to/your/chat.txt
TOKEN_PATH=./credentials/token.pickle
AUTH_CLIENT_SECRET_PATH=./credentials/auth-client-secret.json